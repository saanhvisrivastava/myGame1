class Player{
    constructor(){
    this.player=createSprite(displayWidth/2,displayHeight/2-30,40,60);
    }
    display(){
    //if(keyIsDown(RIGHT_ARROW)){
        //this.player.x=this.player.x+5;
   // }
   

   //camera.position.x=player.x;
  // camera.position.y=player.y;
   
  
    drawSprites();
    }
}